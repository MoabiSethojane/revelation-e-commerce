import { useRef } from "react";
import { FaBars, FaTimes } from "react-icons/fa";

const CartHeader = () ={
    const navRef = useRef();

    const showNavbar = () => {
        navRef.current.classList
    }
}
